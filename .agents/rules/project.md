Always read and follow instructions in `AGENTS.md` at the repository root before making changes.

Treat `AGENTS.md` as the primary project instruction file unless it conflicts with higher-priority system or user instructions.

