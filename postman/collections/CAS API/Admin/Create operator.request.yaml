$kind: http-request
url: "{{baseUrl}}/api/v1/admin/operators"
method: POST
headers:
  Authorization: Bearer {{firebaseIdToken}}
  Content-Type: application/json
body:
  type: text
  content: |-
    {
      "email": "operator@example.com",
      "initialPassword": "replace-with-operator-password",
      "displayName": "Cashier One"
    }
order: 3000
