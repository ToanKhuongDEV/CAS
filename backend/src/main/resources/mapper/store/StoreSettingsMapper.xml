<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="vn.cas.store.mapper.StoreSettingsMapper">

    <select id="findLongWaitWarningMinutesByStoreId" resultType="java.lang.Integer">
        SELECT long_wait_warning_minutes
        FROM stores
        WHERE id = #{storeId}
    </select>

    <update id="updateLongWaitWarningMinutes">
        UPDATE stores
        SET long_wait_warning_minutes = #{longWaitWarningMinutes}
        WHERE id = #{storeId}
    </update>

    <select id="findByStoreId" resultType="vn.cas.store.model.StoreSettings">
        SELECT id, name, address, phone, email, logo_url AS logoUrl,
               logo_storage_key AS logoStorageKey,
               google_maps_location AS googleMapsLocation, open_time AS openTime,
               close_time AS closeTime, welcome_slogan AS welcomeSlogan, status
        FROM stores WHERE id = #{storeId}
    </select>

    <select id="findActiveByStoreId" resultType="vn.cas.store.model.StoreSettings">
        SELECT id, name, address, phone, email, logo_url AS logoUrl,
               logo_storage_key AS logoStorageKey,
               google_maps_location AS googleMapsLocation, open_time AS openTime,
               close_time AS closeTime, welcome_slogan AS welcomeSlogan, status
        FROM stores WHERE id = #{storeId} AND status = 'ACTIVE'
    </select>

    <update id="updateStoreSettings">
        UPDATE stores
        SET name = #{name}, address = #{address}, phone = #{phone}, email = #{email},
            logo_url = #{logoUrl}, logo_storage_key = #{logoStorageKey},
            google_maps_location = #{googleMapsLocation},
            open_time = #{openTime}, close_time = #{closeTime},
            welcome_slogan = #{welcomeSlogan}, status = #{status}
        WHERE id = #{storeId}
    </update>
</mapper>
