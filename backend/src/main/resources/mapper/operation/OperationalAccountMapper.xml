<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="vn.cas.operation.infrastructure.persistence.mybatis.OperationalAccountMapper">

    <select id="findActiveByFirebaseUid" resultType="vn.cas.operation.domain.OperationalAccount">
        SELECT id,
               store_id,
               firebase_uid,
               display_name,
               role
        FROM accounts
        WHERE firebase_uid = #{firebaseUid}
          AND status = 'ACTIVE'
    </select>

    <update id="updateLastLoginAt">
        UPDATE accounts
        SET last_login_at = #{lastLoginAt}
        WHERE id = #{accountId}
          AND status = 'ACTIVE'
    </update>
</mapper>
