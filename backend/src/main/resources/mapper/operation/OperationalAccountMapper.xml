<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="vn.cas.operation.infrastructure.persistence.mybatis.OperationalAccountMapper">

    <select id="findActiveByFirebaseUid" resultType="vn.cas.operation.domain.OperationalAccount">
        SELECT id,
               store_id,
               firebase_uid,
               display_name,
               role
        FROM accounts
        WHERE firebase_uid = #{firebaseUid}
          AND status = 'ACTIVE'
    </select>

    <update id="updateLastLoginAt">
        UPDATE accounts
        SET last_login_at = #{lastLoginAt}
        WHERE id = #{accountId}
          AND status = 'ACTIVE'
    </update>

    <select id="existsByFirebaseUid" resultType="boolean">
        SELECT EXISTS(SELECT 1 FROM accounts WHERE firebase_uid = #{firebaseUid})
    </select>

    <insert id="insertOperatorAccount" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO accounts (store_id, firebase_uid, display_name, role, status)
        VALUES (#{storeId}, #{firebaseUid}, #{displayName}, 'OPERATOR', 'ACTIVE')
    </insert>

    <insert id="insertAdminAccount" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO accounts (store_id, firebase_uid, display_name, role, status)
        VALUES (#{storeId}, #{firebaseUid}, #{displayName}, 'ADMIN', 'ACTIVE')
    </insert>

    <update id="deactivateOperatorAccount">
        UPDATE accounts
        SET status = 'INACTIVE'
        WHERE id = #{accountId}
          AND store_id = #{storeId}
          AND role = 'OPERATOR'
          AND status = 'ACTIVE'
    </update>
</mapper>
